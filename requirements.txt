# Python runtime dependencies for flowpose_request_ik_tester.py.
#
# Python 3.10 and CUDA 11.8 are selected in environment.yml.  The additional
# PyTorch wheel index is needed for the CUDA-enabled builds below.
--extra-index-url https://download.pytorch.org/whl/cu118

# GPU inference stack (kept at the versions used by FlowPose).
torch==2.1.0+cu118
torchvision==0.16.0+cu118
torchaudio==2.1.0+cu118

# Application, RGB-D camera, configuration, and visualization.
numpy>=1.26,<2
opencv-python>=4.8,<4.12
Pillow>=9.5
PyYAML>=6.0
pyrealsense2==2.55.1.6486

# FlowPose runtime imports.
ipdb>=0.13
scikit-learn>=1.3,<1.6
scipy>=1.10,<1.16
tensorboardX==2.6.2.2
webdataset>=0.2.86,<2

# SAM3 runtime imports.  SAM3 itself is installed from its source checkout;
# see ENVIRONMENT.md because the application accepts that path at runtime.
timm>=1.0.17
ftfy==6.1.1
regex>=2023.0
iopath>=0.1.10
typing_extensions>=4.8
huggingface_hub>=0.25

# Daimon gripper receiver and generated protobuf client.
grpcio>=1.75.1
protobuf>=6.32.1
-e ./daimon_stuff/dm_gripper_py

# FlowPose's CUDA extension still uses setup.py during compilation.
setuptools<81
wheel
ninja

# Test-only dependency used by this repository's test suite.
pytest>=7,<9
