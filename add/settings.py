"""Feature switches for the optional browser and voice interfaces."""

# These are the project defaults.  Command-line flags can override them for a
# particular run with --no-tablet-ui and --no-voice-enabled.
WEB_ENABLED = True
VOICE_ENABLED = True
